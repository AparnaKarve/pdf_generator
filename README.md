# PDF generation with `pdf-creator-node`

Steps -
- Run `npm install` to install the required packages
- Run  `node server` to generate the PDF file. The file would be generated as `./pdf_output/myplans.pdf`

